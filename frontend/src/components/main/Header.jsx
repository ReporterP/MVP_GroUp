import React from 'react';
import Logo from '../main/UI/Logo';

const Header = props => {

  return (
    <header>
      <div className="container">
        <Logo/>
      </div>
    </header>
  );
}

export default Header;
